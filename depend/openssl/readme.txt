Version of attached binary OpenSSL: 1.1.0f
Supported development environments: Linux, macOS

To build a custom version of OpenSSL run build_X.sh (where 'X' is a destination platform name). There is no need for OpenSSL on iOS platform, because we use OS built-in TLS service.
